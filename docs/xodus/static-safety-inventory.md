# Static Safety Inventory

## Scope

This inventory covers Rust production sources under `crates/` and supports the Phase 2 requirement that remotely influenced parser, package, service, and environment paths do not contain placeholder macros or unconditional process aborts.

The inventory was refreshed on August 25, 2026 after the parser and service hardening checkpoints `070eb42`, `658beef`, `e9eab1a`, `7f9d68e`, `65b9274`, `59b37c5`, `c7e6347`, `d474620`, `d002e3e`, and `b8a998e`.

## Scan

The baseline search was:

```text
rg -n --glob '*.rs' '(TODO|todo!|unimplemented!|panic!|unwrap\(|expect\()' crates
```

The result contains no `TODO`, `todo!`, or `unimplemented!` match in the current source tree. The remaining `panic!` and `expect` matches are in `#[cfg(test)]` modules and are assertion failures for synthetic fixtures, test setup, or authorized opt in account tests. They are not reachable from production command, service, parser, package, or environment paths.

## Reviewed production boundaries

| Boundary | Result | Evidence |
| --- | --- | --- |
| XVD header and metadata parsing | Typed failures for invalid content type, unsupported XVC versions and keys, bounded XVC and package file counts, bounds, arithmetic, I/O, and allocation. Region-header, encrypted-section, package-file, and segment-file map reservations are fallible before insertion. Invalid content type is rejected before metadata access. Local extraction verifies a synthetic page hash before output and retries transient output failures through success. | `crates/msixvc/src/xvd.rs`, `XvdFileParseError`, `XvdFileParseError::RegionHeaderAllocationFailed`, `XvdFileParseError::RegionSectionAllocationFailed`, `UserPackageFilesParseError::FileCountTooLarge`, `UserPackageFilesParseError::FileAllocationFailed`, `SegmentMetadataParseError::FileMapAllocationFailed`, `parse_rejects_invalid_content_type_before_metadata_access`, `parse_user_package_files_rejects_oversized_table_before_entry_reads`, `extract_file_verifies_page_hash_and_retries_output`, `extract_file_rejects_page_hash_mismatch_before_output` |
| NTFS stream layout inspection | Report, filtered-report, and nonresident data-run vectors reserve fallibly before insertion. Attribute-list synthesis caps its temporary buffer at 4,096 bytes and reserves it fallibly. Checked run merging returns typed invalid-data failures on start or length overflow. | `crates/msixvc/src/streaming_ntfs.rs`, `append_run_segment`, `append_run_segment_merges_contiguous_runs`, `append_run_segment_rejects_start_overflow`, `append_run_segment_rejects_length_overflow` |
| XVD enum wire parsing | Four byte wire values are validated at their full width, so out of domain values cannot alias valid one byte enum values. | `crates/msixvc/src/models/xvd/enums.rs`, `xvd_type_rejects_values_outside_the_wire_domain`, `xvd_content_type_rejects_values_outside_the_wire_domain` |
| XSP parsing and apply | Typed header, record, range, hash, ordering, space, and I/O failures. Forward and rollback byte and async stream helpers verify source and target hashes, while truncated-source, truncated-new-data, and target-mismatch cases fail before an invalid result is exposed. Large XVD base inputs are checked through their bounded header so content identity and source package version must match the XSP before staging, while small synthetic inputs remain supported by the fixture path. The credential-free CLI integration tests apply a valid forward fixture, replace an existing active file through rollback, reject a target-hash-mismatched replacement, reject a malformed XVD base before staging, recover a process crash after staged output, run after an MSIXVC2 replacement in the same destination, preserve the active output, and remove failed staging. Mutated inputs return a result without a process panic. | `crates/msixvc/src/xsp.rs`, `crates/msixvc/src/xvd.rs`, `crates/xodus-cli/src/commands/apply_xsp.rs`, `applies_valid_fixture_and_preserves_active_state_after_failed_update`, `applies_rollback_fixture_replaces_existing_active_state_transactionally`, `applies_xsp_after_msixvc2_replacement_preserves_package_state`, `rejects_malformed_xvd_base_before_staging`, `xsp_process_crash_recovers_staged_update`, `applies_a_bounded_rollback_to_bytes`, `applies_stream_rollback_with_source_and_target_hashes`, `stream_update_rejects_truncated_source_before_output`, `stream_update_rejects_truncated_new_data_before_output`, `stream_update_rejects_target_hash_mismatch_after_staging`, `mutated_xsp_fixture_never_panics` |
| CLEP challenge array layout | The only CLEP byte-array reshapes are isolated in the challenge cipher. Packed `ClepV2` and `ClepV4` are asserted to be 2,048 bytes with byte alignment, and the obfuscation and deobfuscation round trip preserves both layouts. | `crates/xodus/src/clep/challenge.rs`, `clep_layouts_match_the_2048_byte_reshape`, `test_deobfuscation_round_trip` |
| MSIXVC2 inspection and install | Typed ZIP, path, duplicate, symbolic link, compression, metadata, size, entry metadata lookup, and transaction failures. The credential-free CLI integration tests install a base fixture, apply an update fixture, reject an integrity-mismatched replacement, and recover a process crash after staged extraction while preserving the active state and removing failed staging. Mutated archives return a result without a process panic. | `crates/msixvc/src/msixvc2.rs`, `crates/xodus-cli/src/commands/install_msixvc2.rs`, `installs_update_fixture_and_preserves_active_state_after_failed_replacement`, `msixvc2_process_crash_recovers_staged_install`, `mutated_msixvc2_fixture_never_panics`, `missing_entry_metadata_returns_typed_error` |
| Package paths and promotion | Hostile paths fail before output creation, package metadata and hash manifests and transaction journals are bounded before collection, MSIXVC2 install path indexes and stale-path collections use fallible reservations, Linux writes stay beneath the transaction root, the per-destination lock is opened relative to a no-symlink root descriptor, direct short downloads and complete hash mismatches preserve an existing verified package without a leftover staging directory, direct download process-crash recovery removes a partial staged body without touching the verified package, and promotion rollback preserves the prior verified state across journaled backup-pending, backup, staged-rename, process-crash, and promotion interruption points. The streaming CLI also returns failure and drops in-flight work on `Ctrl+C` before promotion. | `crates/xodus-cli/src/commands/streaming.rs` tests, `transaction_lock_rejects_symlink_escape`, `streaming_cancellation_drops_operation_before_promotion`, `transaction_recovery_restores_after_injected_promotion_interruptions`, `transaction_recovery_restores_after_process_crash_injection`, `crates/xodus-cli/src/commands/install_msixvc2.rs`, `crates/xodus-cli/src/commands/apply_xsp.rs`, `crates/xodus-cli/src/commands/download.rs`, `short_download_retries_before_atomic_promotion`, `hash_mismatch_preserves_existing_package_and_cleans_staging`, `download_process_crash_recovers_staged_partial_package` |
| Versioned package metadata retrieval | The package service supports both latest metadata and an exact `GetSpecificBasePackage` revision route. Content and version identifiers are added as URL path segments, with empty and control-bearing values rejected before authentication or network activity. The version selector supplies a source-revision acquisition boundary for later XSP transactions but does not infer a source identity from an update filename or claim a real update. | `crates/xodus-cli/src/package.rs`, `package_endpoint_url_selects_latest_or_specific_route`, `package_endpoint_url_encodes_path_delimiters_inside_ids`, `package_endpoint_url_rejects_empty_or_controlled_ids` |
| HTTP and cache readers | Typed status, range, position, length, response extent, retry, and premature EOF failures with bounded retry budgets. Active response cursors reject chunks beyond the exact declared `Content-Range` end before buffering. Prefix cache target-end arithmetic uses checked `usize` to `u64` conversion and declared-length subtraction before cache reads or upstream buffering. Direct reader tests reject a resumed response whose `Content-Length` disagrees with its inclusive `Content-Range` before activating that response. Direct XVD HTTP tests verify a local partial response range, promote only a page with a valid truncated hash, and reject a mismatched page before output. | `crates/msixvc/src/streaming.rs` tests, `crates/msixvc/src/xvd.rs` tests, `http_read_rejects_chunk_beyond_response_extent`, `http_read_rejects_resumed_content_length_mismatch`, `prefix_target_end_stays_within_declared_length`, `prefix_target_end_rejects_position_beyond_length`, `download_file_http_verifies_page_hash_before_promotion`, `download_file_http_rejects_page_hash_mismatch_before_output` |
| HTTP extent properties | A deterministic seeded sweep covers 4,096 bounded position, active-offset, and response-extent cases, including valid sums and invalid overlong or mismatched extents. | `http_read_extent_properties_hold_for_seeded_inputs` |
| Parser fuzz harness | An explicit `cargo-fuzz` target drives the MSIXVC2, XSP, and XVD parsers from bounded arbitrary bytes without credentials, network access, filesystem writes, or executable loading. The August 25, 2026 nightly coverage-guided campaign at signed checkpoint `d3235a3` completed 10,000 executions over 11 tracked MSIXVC2 and XSP fixture files, reached 1,632 inline coverage counters and 4,758 feature counters, added 365 new units, and produced no sanitizer failure or crash artifact. The corpus was copied into an exact disposable directory for the run and removed after completion, and no generated corpus or crash artifact was retained. | `fuzz/fuzz_targets/parse_inputs.rs`; populate an exact disposable corpus directory with `crates/msixvc/testdata/msixvc2/*` and `crates/msixvc/testdata/xsp/*`, then run `RUSTUP_TOOLCHAIN=nightly cargo fuzz run parse_inputs <disposable-corpus> -- -runs=10000 -timeout=5 -print_final_stats=1` from the repository root |
| Account and licensing responses | Typed HTTP status, bounded Xbox title management, authentication, and package JSON, device credential XML, display catalog JSON, token store values, plus license response and value bodies, schema, empty collection, token conversion, key, license, and UTF 8 failures. RST responses now reject non success HTTP status before XML parsing or token processing. Device credential setup distinguishes missing state from corrupted storage, propagates provisioning and reauthentication failures, and prevents CLI or service false-success startup. Silent refresh propagates persistent STS token write failures instead of returning a successful exchange with an unsaved session. Title endpoint selection filters the bounded decoded response as an iterator without a second attacker sized collection. Catalog subproduct resolution rejects cycles and limits redirect depth before unbounded recursion. Test only assertions remain outside production paths. | `crates/xodus/src/api/xbox/auth.rs`, `crates/xodus/src/api/xbox/title.rs`, `crates/xodus-cli/src/package.rs`, `crates/xodus/src/api/live/mod.rs`, `crates/xodus/src/api/live/rst/request.rs`, `crates/xodus/src/tokens/device.rs`, `crates/xodus/src/api/displaycatalog.rs`, `crates/xodus/src/tokens/store.rs`, `crates/xodus/src/tokens/manager.rs`, `crates/xodus/src/licensing/content.rs`, `register_content_id_redirect`, `RSTRequest::request`, `refreshed_token_persistence_failure_is_returned`, `oversized_xbox_response_length_is_rejected_before_json_decode`, `oversized_device_credential_response_is_rejected_before_decode`, `memory_backend_rejects_oversized_values_before_storage`, `declared_oversized_response_is_rejected`, `oversized_license_response_is_rejected_before_json_decode`, `oversized_license_value_is_rejected_before_decode` |
| SPLicense binary decoding | Base64 input and TLV payloads are bounded before allocation, short signature blocks fail without subtraction underflow, packed content key identifiers and 40 byte key lengths are validated, malformed SOAP token fragment references fail instead of indexing, and SOAP response bodies are bounded before XML decoding. | `crates/xodus/src/licensing/splicense.rs`, `crates/xodus/src/api/live/rst/request.rs` |
| RST request construction | Empty scope policy input returns a typed builder error before token removal, so malformed or incomplete callers cannot trigger an empty vector panic. | `crates/xodus/src/api/live/rst/builder.rs`, `RSTBuilderError::MissingScopePolicy` |
| Hardware probing | Linux reads the kernel SMBIOS file directly without an interactive privilege subprocess, bounds collection before parsing, rejects malformed headers, string indexes, and UUID ranges with typed I/O errors, and reports unavailable disk serial identity instead of a fabricated constant. | `crates/xodus/src/hardware.rs`, `read_bounded_smbios`, `parse_smbios`, `provisioning_does_not_fabricate_a_disk_serial` |
| Service IPC | Bounded payloads, deadlines, connection and rate limits, peer identity checks, absolute private runtime directory validation, explicit unsupported operations, escaped machine-readable errors, and bounded control-free MSA client identifiers validated before account-state or token access. | `crates/xodus-service/src/connection`, `crates/xodus-service/src/main.rs`, `validate_msa_client_id`, and `invalid_msa_client_id_fails_before_account_state_access` |

## Test-only matches

The scan still reports `panic!` and `expect` in test modules. These intentionally fail a test when a synthetic fixture violates the test's expected contract. They are not used as production fallback behavior. The ignored account-backed tests also retain assertions because they are opt in and require authorized external state; they are excluded from ordinary offline verification and do not authorize a release claim.

## Limits

This is a static inventory. It does not prove dynamic behavior, external service compatibility, protected executable launch, or target game lifecycle completion. Mutation regressions, bounded fixture tests, the fuzz harness runtime, workspace Clippy, account-backed verification, runtime traces, and real target exercises remain separate required evidence. The bounded campaign produced no crash corpus, so no crash-specific regression is claimed beyond the existing deterministic parser and transaction tests.
