# Static Safety Inventory

## Scope

This inventory covers Rust production sources under `crates/` and supports the Phase 2 requirement that remotely influenced parser, package, service, and environment paths do not contain placeholder macros or unconditional process aborts. The fuzz evidence was refreshed at signed checkpoint `3c0a09e`.

The inventory was refreshed on August 25, 2026 after the parser, service, transport, cursor, package capacity, and local launch hardening checkpoints `070eb42`, `658beef`, `e9eab1a`, `7f9d68e`, `65b9274`, `59b37c5`, `c7e6347`, `d474620`, `d002e3e`, `b8a998e`, `e37a3a3`, `ecdfb0b`, `20d5466`, `0856c3b`, `c0d85aa`, `cbe2157`, `a1c1609`, `2b05894`, `5ba0c47`, `416d412`, `b8dd35b`, `49c3711`, `05e9a30`, `fb97bc4`, `7d56e15`, `1e07267`, and the current working checkpoint.

On August 26, 2026, the current parser checkpoint `729175e` completed a bounded nightly cargo fuzz campaign of 10,000 executions against the 11 tracked MSIXVC2 and XSP fixtures. Final coverage was 1,693 with a feature count of 4,968, 337 non-crash units were added, peak resident set size was 405 MiB, and no crash artifact or sanitizer failure was reported. The exact disposable corpus and generated artifacts were removed after completion.

On August 26, 2026, the current checkout checkpoint `3c0a09e` completed another bounded nightly cargo fuzz campaign of 10,000 executions against the same 11 tracked MSIXVC2 and XSP fixtures. Final coverage was 1,629 with a feature count of 4,222, 308 non-crash units were added, peak resident set size was 384 MiB, and no crash artifact or sanitizer failure was reported. The exact disposable corpus and generated artifacts were removed after completion.

Checkpoint `2b05894` removes the final production `debug_assert!` paths from the reviewed parser and CLEP code. Checkpoint `5ba0c47` adds a checked twenty percent free space reserve before direct package downloads, checkpoint `416d412` applies the same reserve to the remote streaming cache and changed-file writes, checkpoint `b8dd35b` applies it to XSP updates after lock and recovery, checkpoint `49c3711` binds metadata selected streaming responses to their declared package length before parsing, checkpoint `05e9a30` rejects negative package file sizes before direct download URL construction, checkpoint `fb97bc4` validates returned package identity and file metadata before selection or CDN URL construction, checkpoint `7d56e15` rejects duplicate package file names before downstream selection, and checkpoint `1e07267` validates nonempty package file hashes as 32 byte SHA-256 base64 digests at the same metadata boundary while preserving empty hash behavior. Local formatting, workspace tests, zero warning Clippy, and the release build pass. Hosted run [32924183997](https://github.com/EnVisione/xodus/actions/runs/32924183997) passes formatting plus Linux x86_64 and macOS arm64 checks for branch checkpoint `21cece1`, which contains `05e9a30`. Hosted run [32926270780](https://github.com/EnVisione/xodus/actions/runs/32926270780) passes the same checks for documentation checkpoint `08e3710`, which contains `fb97bc4`. Hosted run [32926667811](https://github.com/EnVisione/xodus/actions/runs/32926667811) passes the same checks for documentation checkpoint `80a55d7`, which contains `7d56e15`. Hosted run [32927381576](https://github.com/EnVisione/xodus/actions/runs/32927381576) passes the same checks for documentation checkpoint `fbf3687`, which contains `1e07267`.

On August 26, 2026, the complete workspace all-target all-feature test command without ignored account tests, `cargo test --workspace --all-targets --all-features -- --test-threads=1`, passed 443 tests across `msixvc`, `msixvc-common`, `xal`, `xodus`, `xodus-cli`, and `xodus-service`, with two ignored tests. The later authorized account-inclusive rerun returned `user STS token is unavailable: entry not found` from `auth::test_minecraft_win_auth`, so the latest Minecraft authorization gate is not green. These are repository and account smoke-test results only; they do not prove protected title installation, runtime, update, or release behavior. Hosted run [32936266989](https://github.com/EnVisione/xodus/actions/runs/32936266989) also passed formatting, Linux x86_64, and macOS arm64 checks for checkpoint `17d0f40`.

The same authorized Minecraft test passes when run alone, including after the typed error change, but the account-inclusive harness still encounters missing persisted user state. This difference is recorded as an account test isolation limitation rather than a successful full-suite claim.

The current package-root boundary replaces ordinary recursive directory creation in the streaming, XSP update, and MSIXVC2 install commands. Linux creates each destination component through a no-symlink directory descriptor before locking, so a hostile symlinked parent cannot redirect pre-lock directory creation. Existing roots are opened with a directory-only flag, so special files cannot be opened as blocking package roots before their type is checked. The focused CLI transaction suite passed 35 tests, including missing-component creation, non-directory rejection, and symlinked-parent rejection. Checkpoint `42c4e66` also makes each streaming prefix cache file use exclusive creation, so a preexisting cache symlink cannot truncate or redirect writes to its target. The focused cache regression passed on Linux. This remains local boundary evidence and does not prove a retail package lifecycle.

The focused `HttpRead` stream and response extent command, `cargo test -p msixvc --lib http_read -- --test-threads=1`, passed 28 tests on August 26, 2026. The extraction integrity tests passed 2 tests, the output retry policy test passed 1 test, the transaction and recovery tests passed 26 tests, and the service protocol and socket tests passed 15 tests. These focused results validate the current bounded slices but do not substitute for authorized package or title lifecycle evidence.

The current `HttpRead` implementation now validates pending chunk offsets before slicing, checked logical and active stream positions, response extent and content length, resumed start and total identity, bounded retry and premature end behavior, and stable HTTPS response policy. Its focused test group passed 31 tests on August 26, 2026, including seeded extent properties and deterministic invalid cases. This is local stream safety evidence only and does not prove a real package transfer, update, install, or title lifecycle.

Checkpoint `4981c70` redacts service request parse errors and records only stable error kinds for routed failures. After that change, `cargo test --workspace --all-targets -- --include-ignored --test-threads=1` passed with zero failures, `cargo clippy --workspace --all-targets --all-features --message-format short -- -D warnings` passed with zero warnings, and `cargo build --workspace --release` completed successfully. These are local repository gates and do not close the external package, entitlement, runtime, or release requirements.

Checkpoint `815a5d7` applies the XSP free space reserve to the actual target output size as well as the descriptor declaration. The CLI checks target hash count conversion and block-size multiplication before staging, then reserves twenty percent over the larger checked value. The regression covers a target output larger than declared disk space and arithmetic overflow. Formatting, the focused XSP suite, workspace tests, zero-warning Clippy, the release build, CodeGraph synchronization, and hosted verification passed. This remains synthetic transaction evidence and does not prove an authorized retail update.

Checkpoint `792f193` also validates the final response URL for streamed HTTP reads. An HTTPS package request now rejects a followed redirect that downgrades to HTTP before accepting status, range, or payload data. The regression test passes with the streaming suite, while HTTP test fixtures and explicitly supplied HTTP sources remain available for local diagnostics.

## Scan

The baseline search was:

```text
rg -n --glob '*.rs' '(TODO|todo!|unimplemented!|panic!|unwrap\(|expect\()' crates
```

The result contains no `TODO`, `todo!`, or `unimplemented!` match in the current source tree. The remaining `panic!`, `expect`, and `debug_assert!` matches are in `#[cfg(test)]` modules and are assertion failures for synthetic fixtures, test setup, or authorized opt in account tests. They are not reachable from production command, service, parser, package, or environment paths.

The parser and CLEP hardening pass also removes the last production debug assertions. The package-file table cursor now returns `UserPackageFilesParseError::PackageFilesTableCursorBeyondEnd` if an internal cursor ever exceeds its validated extent, and the fixed 2,048 byte CLEP array uses its type-level chunk guarantee without a runtime assertion. The deterministic regression tests `package_files_table_cursor_rejects_past_end_without_panicking` and `package_files_table_cursor_accepts_exact_end` cover the new parser boundary.

## Reviewed production boundaries

| Boundary | Result | Evidence |
| --- | --- | --- |
| XVD header and metadata parsing | Typed failures for invalid content type, unsupported XVC versions and keys, bounded XVC and package file counts, fixed-header extent checks, header-length checks, bounds, arithmetic, I/O, and allocation. The path-based parser rejects non-regular inputs before opening them, so device or special-file paths cannot become blocking parse streams. Region-header, encrypted-section, package-file, segment-file map, segment path buffer, and copied segment-hash reservations are fallible before insertion, reading, or cloning, and duplicate segment paths are rejected before map growth. Invalid content type and fixed package metadata headers are rejected before metadata-directed reads. Local extraction verifies a synthetic page hash before output and retries transient output failures through success. | `crates/msixvc/src/xvd.rs`, `XvdFileParseError`, `XvdFileParseError::RegionHeaderAllocationFailed`, `XvdFileParseError::RegionSectionAllocationFailed`, `UserPackageFilesParseError::UserDataHeaderBeyondUserData`, `UserPackageFilesParseError::UserDataHeaderTooSmall`, `UserPackageFilesParseError::FileCountTooLarge`, `UserPackageFilesParseError::FileAllocationFailed`, `SegmentMetadataParseError::SegmentMetadataHeaderBeyondDeclaredLength`, `SegmentMetadataParseError::SegmentMetadataHeaderTooSmall`, `SegmentMetadataParseError::PathAllocationFailed`, `SegmentMetadataParseError::FileMapAllocationFailed`, `SegmentMetadataParseError::HashAllocationFailed`, `SegmentMetadataParseError::DuplicateFileName`, `PopulateSegmentHashesError::HashAllocationFailed`, `parse_file_rejects_non_regular_inputs_before_opening`, `parse_rejects_invalid_content_type_before_metadata_access`, `parse_user_package_files_rejects_a_short_declared_header_before_io`, `parse_user_package_files_rejects_a_short_header_length_before_package_reads`, `parse_user_package_files_rejects_oversized_table_before_entry_reads`, `parse_segment_metadata_rejects_a_short_declared_header_before_io`, `parse_segment_metadata_rejects_a_short_header_length_before_table_reads`, `parse_segment_metadata_rejects_duplicate_paths`, `segment_path_reservation_is_bounded_by_declared_length`, `extract_file_verifies_page_hash_and_retries_output`, `extract_file_rejects_page_hash_mismatch_before_output` |
| NTFS stream layout inspection | Report, filtered-report, and nonresident data-run vectors reserve fallibly before insertion. Attribute-list synthesis caps its temporary buffer at 4,096 bytes and reserves it fallibly. Checked run merging returns typed invalid-data failures on start or length overflow. | `crates/msixvc/src/streaming_ntfs.rs`, `append_run_segment`, `append_run_segment_merges_contiguous_runs`, `append_run_segment_rejects_start_overflow`, `append_run_segment_rejects_length_overflow` |
| XVD enum wire parsing | Four byte wire values are validated at their full width, so out of domain values cannot alias valid one byte enum values. | `crates/msixvc/src/models/xvd/enums.rs`, `xvd_type_rejects_values_outside_the_wire_domain`, `xvd_content_type_rejects_values_outside_the_wire_domain` |
| XSP parsing and apply | Typed header, record, range, hash, ordering, space, allocation, and I/O failures. Forward and rollback byte and async stream helpers verify source and target hashes, reject truncated or trailing base and new-data payloads, reject target output larger than available space, and reject target mismatches before an invalid result is exposed to the transaction layer. Large XVD base inputs are checked through their bounded header so content identity and source package version must match the XSP before staging, while small synthetic inputs remain supported by the fixture path. Descriptor, base, new-data, and hash-manifest paths must resolve to regular files before opening, preventing directories or special inputs from becoming blocking update streams. Hash manifests are bounded before decoding and reserve their entry vector fallibly from the bounded text size. In-memory output and async stream block buffers reserve fallibly after their 64 MiB cap, returning typed allocation failures under memory pressure. The credential-free CLI integration tests apply a valid forward fixture, replace an existing active file through rollback, reject a target-hash-mismatched replacement, reject a malformed XVD base before staging, recover a process crash after staged output, run after an MSIXVC2 replacement in the same destination, preserve the active output, and remove failed staging. XSP capacity is measured after lock and recovery and requires the declared disk space plus a checked twenty percent reserve, with typed storage and overflow failures before staging. Mutated inputs return a result without a process panic. | `crates/msixvc/src/xsp.rs`, `crates/msixvc/src/xvd.rs`, `crates/xodus-cli/src/commands/apply_xsp.rs`, `open_regular_input`, `open_regular_manifest`, `xsp_input_open_rejects_non_regular_files_before_opening`, `bounded_hash_manifest_reader_rejects_non_regular_inputs`, `XspUpdateApplyError::OutputAllocationFailed`, `XspUpdateApplyError::BaseImageTooLong`, `XspUpdateApplyError::NewDataTooLong`, `XspStreamApplyError::BlockAllocationFailed`, `XspStreamApplyError::SourceImageTooLong`, `XspStreamApplyError::NewDataTooLong`, `applies_valid_fixture_and_preserves_active_state_after_failed_update`, `applies_rollback_fixture_replaces_existing_active_state_transactionally`, `applies_xsp_after_msixvc2_replacement_preserves_package_state`, `rejects_malformed_xvd_base_before_staging`, `xsp_process_crash_recovers_staged_update`, `xsp_space_validation_requires_a_twenty_percent_reserve`, `rejects_target_output_larger_than_available_space`, `rejects_trailing_base_bytes_in_byte_apply`, `applies_a_bounded_rollback_to_bytes`, `applies_stream_rollback_with_source_and_target_hashes`, `stream_update_rejects_trailing_base_bytes_before_output`, `stream_update_rejects_truncated_source_before_output`, `stream_update_rejects_truncated_new_data_before_output`, `stream_update_rejects_trailing_new_data`, `rejects_trailing_new_data_in_byte_apply`, `stream_update_rejects_target_hash_mismatch_after_staging`, `mutated_xsp_fixture_never_panics`, `parses_bounded_hash_manifest_entries` |
| CLEP challenge array layout | The only CLEP byte-array reshapes are isolated in the challenge cipher. Packed `ClepV2` and `ClepV4` are asserted to be 2,048 bytes with byte alignment, and runtime block traversal uses checked `as_chunks_mut` slices rather than a mutable layout transmute. The obfuscation and deobfuscation round trip preserves both layouts. | `crates/xodus/src/clep/challenge.rs`, `clep_layouts_match_the_2048_byte_reshape`, `test_deobfuscation_round_trip` |
| MSIXVC2 inspection and install | Typed ZIP, path, duplicate, symbolic link, compression, metadata, size, entry metadata lookup, free space, and transaction failures. Before inspection or staging, the credential-free CLI rejects non-regular archive inputs before opening them and checks destination capacity for the bounded uncompressed archive plus a 20 percent reserve. The integration tests install a base fixture, apply an update fixture, reject an integrity-mismatched replacement, and recover a process crash after staged extraction while preserving the active state and removing failed staging. Mutated archives return a result without a process panic. | `crates/msixvc/src/msixvc2.rs`, `crates/xodus-cli/src/commands/inspect.rs`, `crates/xodus-cli/src/commands/install_msixvc2.rs`, `open_archive`, `archive_open_rejects_non_regular_files_before_reading`, `rejects_non_regular_archive_before_inspection`, `validate_available_space`, `install_space_check_rejects_insufficient_capacity`, `installs_update_fixture_and_preserves_active_state_after_failed_replacement`, `msixvc2_process_crash_recovers_staged_install`, `mutated_msixvc2_fixture_never_panics`, `missing_entry_metadata_returns_typed_error` |
| Package paths and promotion | Hostile paths fail before output creation, package path names are capped at 4,096 bytes and component collection reserves fallibly, package metadata and hash manifests and transaction journals are bounded before collection, their bounded byte readers reserve fallibly, MSIXVC2 install path indexes and stale-path collections use fallible reservations, package roots are created one component at a time through no-symlink directory descriptors before locking, package roots are opened from the current directory with directory-only, no-magic-link, and no-symlink resolution, Linux writes stay beneath the transaction root, the per-destination lock is opened relative to a no-symlink root descriptor, journal decoding rejects duplicate final paths and backup paths that do not match their final path, promotion and rollback renames and removals use validated parent descriptors so staged symlinks and symlinked final parents cannot redirect a move, local `file://` streaming sources are checked as regular files before opening, direct request-timeout, too-early, rate-limit, and server responses stay within the bounded retry policy, direct short downloads and complete hash mismatches preserve an existing verified package without a leftover staging directory, direct download process-crash recovery removes a partial staged body without touching the verified package, and promotion rollback preserves the prior verified state across journaled backup-pending, backup, staged-rename, process-crash, and promotion interruption points. The streaming CLI also returns failure and drops in-flight work on `Ctrl+C` before promotion, and its aggregate progress length uses checked addition so a hostile package total cannot wrap the display counter. | `crates/xodus-cli/src/commands/streaming.rs` tests, `ensure_package_root_refuses_symlinked_parent_before_creation`, `ensure_package_root_creates_missing_components_without_symlinks`, `package_root_rejects_non_directory_without_opening_it_as_a_stream`, `progress_length_rejects_overflow_without_wrapping`, `stream_source_open_rejects_non_regular_files_before_opening`, `package_root_refuses_symlink_escape`, `transaction_lock_rejects_symlink_escape`, `transaction_journal_rejects_duplicate_final_paths`, `transaction_promotion_refuses_staged_symlink`, `transaction_promotion_refuses_symlinked_final_parent`, `streaming_cancellation_drops_operation_before_promotion`, `transaction_recovery_restores_after_injected_promotion_interruptions`, `transaction_recovery_restores_after_process_crash_injection`, `package_paths_reject_oversized_names_before_component_allocation`, `crates/xodus-cli/src/commands/install_msixvc2.rs`, `crates/xodus-cli/src/commands/apply_xsp.rs`, `crates/xodus-cli/src/commands/download.rs`, `package_download_retry_status_policy_is_bounded`, `short_download_retries_before_atomic_promotion`, `hash_mismatch_preserves_existing_package_and_cleans_staging`, `download_process_crash_recovers_staged_partial_package` |
| Local Wine launch environment | The run and remote streaming paths open `.xodus-streaming.msixvc` through the rooted no-symlink descriptor helper before parsing or comparing local cache state. The encrypted-file descriptor vector reserves its parsed count fallibly. Before spawning Wine, the `WINE_DLL_FILE_MAP` and selected NT entrypoint use checked byte-length arithmetic and exact fallible string reservations. A path prefix containing the descriptor map separator is rejected before it can alter map parsing. Missing entrypoints return a typed failure and cleanup continues. | `crates/xodus-cli/src/commands/run.rs`, `crates/xodus-cli/src/commands/streaming.rs`, `open_package_input`, `build_wine_environment`, `run_package_input_rejects_a_symlinked_package_file`, `wine_environment_map_uses_nt_paths_and_selected_entrypoint`, `wine_environment_map_rejects_missing_entrypoint`, `wine_environment_map_rejects_separator_in_path_prefix` |
| Versioned package metadata retrieval | The package service supports both latest metadata and an exact `GetSpecificBasePackage` revision route. Content and version identifiers are added as URL path segments, with empty, control-bearing, or oversized values rejected before authentication or network activity. Returned metadata is checked against the requested content and optional version, requires a found response, bounded nonempty package identity and version, at least one file, matching file identities, nonnegative sizes, nonempty and safe package file names, nonempty delta identifiers, no duplicate file names, valid nonempty 32 byte SHA-256 base64 file hashes or empty hashes, and safe CDN metadata before file selection or URL construction. Both `download` and remote `streaming` accept the version selector, while local `file://` streaming remains offline. The selector supplies a source-revision acquisition boundary for later XSP transactions but does not infer a source identity from an update filename or claim a real update. | `crates/xodus-cli/src/package.rs`, `crates/xodus-cli/src/commands/streaming.rs`, `package_endpoint_url_selects_latest_or_specific_route`, `package_endpoint_url_encodes_path_delimiters_inside_ids`, `package_endpoint_url_rejects_empty_or_controlled_ids`, `package_endpoint_url_rejects_oversized_ids`, `package_response_validation_accepts_matching_latest_identity`, `package_response_validation_accepts_matching_specific_identity`, `package_response_validation_rejects_mismatched_content_id`, `package_response_validation_rejects_mismatched_version_id`, `package_response_validation_rejects_mismatched_file_identity`, `package_response_validation_rejects_negative_file_size`, `package_response_validation_rejects_unsafe_file_names`, `package_response_validation_rejects_duplicate_file_names`, `package_response_validation_rejects_invalid_file_hash` |
| Package revision manifests | An opt-in `download --manifest` output records schema version, content and package version identity, and bounded file metadata after successful promotion. It excludes key material, CDN roots, signed URLs, tokens, and package bytes, rejects mismatched file identity or negative sizes before writing, caps serialized output at 8 MiB, checks the parent through the rooted no-symlink directory boundary, and uses a synchronized temporary file plus atomic rename and parent-directory sync. Dry runs reject the option. | `crates/xodus-cli/src/package_manifest.rs`, `manifest_writes_version_identity_without_secrets_or_cdn_roots`, `manifest_rejects_mismatched_file_identity_before_write`, `manifest_rejects_negative_file_size_before_write`, `manifest_rejects_symlinked_parent_before_write` |
| Package CDN URL construction | CDN roots must be bounded HTTPS directory URLs without credentials, queries, or fragments, and relative package paths reject absolute roots, ambiguous separators, raw or encoded dot and separator escapes, queries, fragments, controls, and oversized values before joining. URL joining preserves the validated origin and deduplicates canonical candidates. Direct and streamed package requests also reject a followed HTTPS to HTTP downgrade before accepting response data. | `crates/xodus-cli/src/package.rs`, `crates/xodus-cli/src/commands/download.rs`, `crates/msixvc/src/streaming.rs`, `package_download_urls_reject_unsafe_relative_paths`, `package_download_urls_reject_invalid_roots`, `package_download_urls_preserve_unique_root_order`, `download_response_scheme_rejects_https_downgrade`, `http_read_rejects_https_downgrade_after_redirect` |
| Package download capacity | Direct package downloads reject negative file size metadata before constructing CDN URLs, including dry runs. They check destination free space before issuing a network request, require the checked package size plus a twenty percent reserve, reject response content encodings other than identity before staging, and return a typed storage or response failure when capacity or byte integrity metadata is invalid. | `crates/xodus-cli/src/commands/download.rs`, `checked_package_file_size`, `package_file_size_rejects_negative_before_url_construction`, `validate_download_space`, `download_space_validation_requires_a_twenty_percent_reserve`, `validate_download_response_encoding`, `download_response_encoding_rejects_transformed_package_bytes` |
| Streaming package capacity | Remote streaming checks capacity for the package cache before staging and for changed segment files before writes, with checked arithmetic and a twenty percent reserve. | `crates/xodus-cli/src/commands/streaming.rs`, `validate_streaming_space`, `streaming_space_validation_requires_a_twenty_percent_reserve` |
| Display catalog request inputs | Product identifiers are encoded as URL path segments, market and language values are encoded as query parameters, and empty, control-bearing, oversized, or excessive language inputs return typed failures before any request or token access. | `crates/xodus/src/api/displaycatalog.rs`, `display_catalog_url_encodes_path_and_query_values`, `display_catalog_url_rejects_empty_control_and_oversized_values`, `display_catalog_url_rejects_excessive_language_input` |
| HTTP and cache readers | Typed status, range, position, length, response extent, retry, and premature EOF failures with bounded retry budgets. Transient request timeout, too early, rate limit, and server statuses retry within the reader or download budget, while permanent client statuses fail immediately. Active response cursors reject chunks beyond the exact declared `Content-Range` end before buffering. Prefix cache target-end arithmetic uses checked `usize` to `u64` conversion and declared-length subtraction before cache reads or upstream buffering. Prefix cache files use exclusive creation, so an existing regular file or symlink cannot be truncated or followed as the transaction cache. Metadata selected remote streams require the initial CDN `Content-Length` to match the package file size before parsing, and resumed responses must retain that total. Direct reader tests reject a resumed response whose `Content-Length` disagrees with its inclusive `Content-Range` before activating that response, reject an initial response whose length disagrees with package metadata, reject logical or active-position addition overflow before advancing a cursor, reject pending-chunk addition overflow, reject a corrupt pending-chunk offset before slicing, preserve cursor continuity when a read only partially consumes a response chunk, reject active cursor drift before polling more bytes, reject cache-position addition overflow, and reject a cache symlink without modifying its target. Direct XVD HTTP tests verify a local partial response range, retry a transient server status before promotion, promote only a page with a valid truncated hash, and reject a mismatched page before output. Ranged downloader response chunks are fed into a page-sized pending buffer in bounded pieces, and an internal over-capacity state returns a typed failure instead of growing the buffer without limit. | `crates/msixvc/src/streaming.rs` tests, `crates/msixvc/src/xvd.rs` tests, `retryable_http_status_policy_excludes_permanent_client_errors`, `http_read_retry_budget_is_bounded`, `http_read_reports_premature_eof`, `http_read_returns_premature_eof_after_budget_exhaustion`, `http_read_retries_a_transient_server_status_before_success`, `download_http_retry_policy_is_bounded_and_preserves_typed_failures`, `download_file_http_retries_transient_status_before_promotion`, `http_read_rejects_chunk_beyond_response_extent`, `http_read_rejects_initial_content_length_mismatch`, `http_read_rejects_resumed_content_length_mismatch`, `http_read_rejects_logical_position_overflow`, `http_read_rejects_active_offset_overflow`, `http_read_rejects_pending_chunk_offset_overflow`, `http_read_rejects_pending_chunk_offset_before_slicing`, `http_read_preserves_cursor_when_copying_partial_pending_chunk`, `http_read_rejects_active_cursor_drift_before_polling`, `prefix_cache_rejects_position_overflow`, `prefix_target_end_stays_within_declared_length`, `prefix_target_end_rejects_position_beyond_length`, `prefix_cache_rejects_symlink_before_writing_target`, `download_file_http_verifies_page_hash_before_promotion`, `download_file_http_rejects_page_hash_mismatch_before_output` |
| HTTP extent properties | A deterministic seeded sweep covers 4,096 bounded position, active-offset, and response-extent cases, including valid sums and invalid overlong or mismatched extents. | `http_read_extent_properties_hold_for_seeded_inputs` |
| Parser fuzz harness | An explicit `cargo-fuzz` target drives the MSIXVC2, XSP, and XVD parsers from bounded arbitrary bytes without credentials, network access, filesystem writes, or executable loading. The August 25, 2026 nightly coverage-guided campaign at signed checkpoint `cbe2157` completed 10,000 executions over 11 tracked MSIXVC2 and XSP fixture files, reached 1,633 inline coverage counters and 4,104 feature counters, added 319 new units, and produced no sanitizer failure or crash artifact. A refreshed campaign against parser checkpoint `2b05894` also completed 10,000 executions over the same 11 fixtures, added 345 new units, and produced no sanitizer failure or crash artifact. A current campaign against branch checkpoint `83a328f` completed 10,000 executions over the same 11 fixtures, reached 1,623 inline coverage counters and 4,867 feature counters, added 403 new units, and produced no sanitizer failure or crash artifact. The August 26, 2026 campaign at signed checkpoint `729175e` completed 10,000 executions over the same 11 fixtures, reached final coverage 1,693 and feature count 4,968, added 337 non-crash units, reached 405 MiB peak resident set size, and produced no sanitizer failure or crash artifact. The August 26, 2026 campaign at signed checkpoint `dc98323` completed 10,000 executions over the same 11 fixtures in two seconds, reached final coverage 1,605 and feature count 4,454, added 251 units, reached 367 MiB peak resident set size, and produced no crash artifact or failure. The August 26, 2026 campaign at signed checkpoint `3c0a09e` completed 10,000 executions over the same 11 fixtures in two seconds, reached final coverage 1,629 and feature count 4,222, added 308 non-crash units, reached 384 MiB peak resident set size, and produced no crash artifact or sanitizer failure. Each corpus was copied into an exact disposable directory for the run and removed after completion, and no generated corpus or crash artifact was retained. | `fuzz/fuzz_targets/parse_inputs.rs`; populate an exact disposable corpus directory with `crates/msixvc/testdata/msixvc2/*` and `crates/msixvc/testdata/xsp/*`, then run `RUSTUP_TOOLCHAIN=nightly cargo fuzz run parse_inputs <disposable-corpus> -- -runs=10000 -timeout=5 -print_final_stats=1` from the repository root |
| Account and licensing responses | Typed HTTP status, bounded Xbox title management, authentication, and package JSON, device credential XML, display catalog JSON, token store values, plus license response and value bodies, schema, empty collection, token conversion, key, license, UTF 8, and response body allocation failures. Fixed HTTPS service calls now reject a followed redirect that downgrades to HTTP before status handling, authentication response parsing, license processing, or token processing. RST responses now reject non success HTTP status before XML parsing or token processing and report bounded body allocation failures. Completed RST response bodies become owned UTF 8 strings without a second body sized clone. Derived key token indexing reserves its response sized map and each copied identifier and nonce fallibly before signature verification or SOAP decryption. Shared key derivation reserves its caller sized material buffer fallibly and returns `SharedKeyError::MaterialAllocationFailed` instead of aborting. Device credential setup distinguishes missing state from corrupted storage, propagates provisioning and reauthentication failures, and prevents CLI or service false-success startup. Silent refresh propagates persistent STS token write failures instead of returning a successful exchange with an unsaved session. The SISU bridge classifies missing persisted user or device STS state, unsupported stored token shapes, and unexpected exchange bodies through typed errors before proceeding. Title endpoint selection filters the bounded decoded response as an iterator without a second attacker sized collection. Catalog subproduct resolution rejects cycles and limits redirect depth before unbounded recursion, and subproduct collection growth uses a fallible reservation while parsing only the expected three part entitlement shape. The standalone SPLicense command derives its key only for validation and reports a fixed status message, never printing decrypted key material. CIK export creates and opens the requested directory through the no-symlink package-root boundary and refuses symlinked CIK paths before writing decrypted key material. An authorized run of `api::live::test::test_get_xbox_live_dev_token` passed on August 26, 2026, proving the current device credential provisioning and Xbox token exchange smoke path; a later account-inclusive rerun returned `user STS token is unavailable: entry not found` from `test_minecraft_win_auth`, so the latest Minecraft authorization gate is not green and no title lifecycle claim is made. Test only assertions remain outside production paths. | `crates/xodus/src/api/xbox/auth.rs`, `crates/xodus/src/api/xbox/title.rs`, `crates/xodus-cli/src/commands/splicense.rs`, `crates/xodus-cli/src/package.rs`, `crates/xodus/src/api/live/mod.rs`, `crates/xodus/src/api/live/rst/request.rs`, `crates/xodus/src/api/live/utils.rs`, `crates/xodus/src/tokens/device.rs`, `crates/xodus/src/api/displaycatalog.rs`, `crates/xodus/src/tokens/store.rs`, `crates/xodus/src/tokens/manager.rs`, `crates/xodus/src/licensing/content.rs`, `crates/xodus-cli/src/commands/license.rs`, `register_content_id_redirect`, `append_subproduct`, `collect_nonces`, `generate_shared_key`, `allocate_shared_key_material`, `response_text_from_body`, `RSTRequest::request`, `persist_refreshed_user_token`, `SisuError`, `do_sisu`, `sisu_reports_missing_user_token_without_network_activity`, `test_get_xbox_live_dev_token`, `test_minecraft_win_auth`, `oversized_xbox_response_length_is_rejected_before_json_decode`, `oversized_device_credential_response_is_bounded_before_decode`, `memory_backend_rejects_oversized_values_before_storage`, `declared_oversized_response_is_rejected`, `oversized_license_response_is_rejected_before_json_decode`, `oversized_license_value_is_rejected_before_decode` |
| Authentication transport dependencies | The vendored XAL source preserves the audited upstream API and request signing code while moving OAuth2 to version 5, Reqwest to version 0.12, http to version 1, h2 to version 0.4, and rustls webpki to version 0.103. `cargo audit` no longer reports the former h2 0.3.27 or rustls webpki 0.101.7 vulnerabilities. | `vendor/xal`, `crates/xodus/Cargo.toml`, `deny.toml`, `cargo audit --no-fetch --stale`, `cargo tree -i h2@0.4.16`, `cargo tree -i rustls-webpki@0.103.13` |
| SPLicense binary decoding | Base64 input and TLV payloads are bounded before allocation, TLV buffers use fallible exact reservations, package-name, packed-content-key, and entry-ID collections report typed allocation failures before mutation, short signature blocks fail without subtraction underflow, packed content key identifiers and 40 byte key lengths are validated, malformed SOAP token fragment references fail instead of indexing, and SOAP response bodies are bounded before XML decoding. | `crates/xodus/src/licensing/splicense.rs`, `SPLicenseDecodeError::AllocationFailed`, `crates/xodus/src/api/live/rst/request.rs` |
| RST request construction | Empty scope policy input returns a typed builder error before token removal, so malformed or incomplete callers cannot trigger an empty vector panic. | `crates/xodus/src/api/live/rst/builder.rs`, `RSTBuilderError::MissingScopePolicy` |
| Hardware probing | Linux reads the kernel SMBIOS file directly without an interactive privilege subprocess, uses fallible reservations for the bounded raw buffer, string table, and copied fields, rejects malformed headers, string indexes, and UUID ranges with typed I/O errors, and reports unavailable disk serial identity instead of a fabricated constant. | `crates/xodus/src/hardware.rs`, `read_bounded_smbios`, `parse_smbios`, `copy_smbios_string`, `provisioning_does_not_fabricate_a_disk_serial` |
| Service IPC | Bounded payloads, deadlines, connection and rate limits, peer identity checks, absolute private runtime directory validation, explicit unsupported operations, escaped machine-readable errors, and bounded control-free MSA client identifiers validated before account-state or token access. XML parse failures are logged with a fixed redacted marker, and routed I/O failures expose only their stable error kind rather than dynamic error text. | `crates/xodus-service/src/connection`, `crates/xodus-service/src/main.rs`, `validate_msa_client_id`, `invalid_msa_client_id_fails_before_account_state_access` |

## Test-only matches

The scan still reports `panic!` and `expect` in test modules. These intentionally fail a test when a synthetic fixture violates the test's expected contract. They are not used as production fallback behavior. The ignored account-backed tests also retain assertions because they are opt in and require authorized external state; they are excluded from ordinary offline verification and do not authorize a release claim.

## Secret scan

A redacted `gitleaks dir` scan over an exact archive of the current `HEAD` tracked tree found no leaks. A redacted `gitleaks git` history scan found one generic API key match in the removed historical file `docs/device.md`, line 34, at commit `536dc7f7f8bdf8183032c2bb8c54e8a999124126`. The historical example labels its values as random, while the current device documentation replaces the password value with `REDACTED`; the historical provenance is not independently verified. No credential value is retained in this inventory. The residual is tracked in [issue 12](https://github.com/EnVisione/xodus/issues/12) and remains open until the stable release security gate confirms its synthetic status or revocation.

## Dependency security review

`cargo audit --no-fetch --stale` on August 25, 2026 scanned 639 dependencies and found one vulnerability, `rsa` 0.9.10 with `RUSTSEC-2023-0071`, the Marvin timing side channel. The advisory reports no fixed upstream release. The all-target feature graph traces the package through `bergshamra-keys` and `kryptering`, but the actual CachyOS Linux graph checked with `cargo tree --workspace --target x86_64-unknown-linux-gnu -e features -i rsa@0.9.10` returns no package and uses AWS-LC instead. The vulnerable package remains a non-Linux RustCrypto dependency in the lockfile. The service exposes the Linux signing path only behind a mode 600 Unix socket with an owning user ID check. `cargo deny check` and Clippy pass. Issue 13 tracks removal or isolation of the non-Linux path before those targets ship; no fixed package version or cross-target remediation has been verified.

## Limits

This is a static inventory. It does not prove dynamic behavior, external service compatibility, protected executable launch, or target game lifecycle completion. Mutation regressions, bounded fixture tests, the fuzz harness runtime, workspace Clippy, account-backed verification, runtime traces, and real target exercises remain separate required evidence. The bounded campaign produced no crash corpus, so no crash-specific regression is claimed beyond the existing deterministic parser and transaction tests.

The prefix cache creates its file exclusively and clones the reader handle from that created file instead of reopening the caller path. This keeps both handles bound to the file that passed exclusive creation even if the directory entry changes afterward.

HTTP package readers reject response content encodings other than identity before accepting lengths, ranges, or payload bytes. This keeps reqwest decompression from changing the byte stream covered by the declared package extent and hash contract. Deterministic tests cover rejection of gzip responses and acceptance of identity or missing encoding.

The ranged XVD downloader applies the same content encoding boundary before validating `Content-Range` or promoting output. Its typed `InvalidResponseContentEncoding` failure and deterministic gzip regression keep direct downloads and streamed reads aligned.

The ordinary CLI package downloader now applies the same identity only policy before creating its staging transaction. This prevents reqwest content decoding from changing the bytes covered by the declared size and optional `FileHash` checks. Its deterministic header tests cover transformed, identity, and missing encoding values.
